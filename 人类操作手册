# WDP AI 开发库 - 人类操作手册

> 本手册面向使用本库的操作者，指导如何正确输入指令让 AI 完成 WDP 相关开发任务。

---

## 一、基础启动指令

### 1. 默认加载、启动工作库

```
阅读 `\WorkFiles_Codex\WDP_AIcoding\skills\wdp-entry-agent\SKILL.md`，了解路由规则和调度机制
```

**重要提示**：
- `wdp-entry-agent` 是整个库的核心入口，负责路由判断和调度
- 每次开始新任务时都应先执行此指令
- 该 Skill 已更新"📋 本文档职责范围"声明，明确了 Entry Agent 只负责路由，不提供具体 API 代码

---

## 二、需求开发流程

### 步骤 1：需求结构化梳理（重要）

如果需要根据需求生成 demo，**必须先进行需求结构化梳理**：

```
先使用 '\WDP_AIcoding\skills\wdp-intent-orchestrator' 帮我结构化梳理下在"XXXXX"路径下的"XXXX"需求文件，并输出一份结构化梳理完成的《系统意图与架构设计报告》.md文件，输出在"XXXXX"路径下。

请注意：
1. 不要在输出后立刻执行代码的编写
2. 需要进一步询问我是否有特定的实现方式
3. 如果涉及空间能力（坐标、位置等），必须给出具体坐标参数
4. 如果涉及对象操作，必须给出 eid 或 nodeid 等信息
```

**为什么必须先梳理？**
- AI 对空间能力很弱，需要明确的坐标和对象标识
- 结构化梳理可以暴露需求中的缺失信息
- 避免后续反复修改

### 步骤 2：编码实现规划

生成梳理过的结构化需求文档后：

```
读取生成的需求分析文件，相关工程所需文件都在和需求文档相同的路径下，浏览注意事项后开始执行编码工作，注意主动高频访问回忆'wdp-entry-agent'等相关技能规范开发，wdp不是一个行业内常见通用的api产品。

注意：
1. 不要拘泥于使用某一技术或框架
2. 结合相关的 api-skill 进行开发规划
3. 给出实现的路径框架
4. 设定为一名专业的前端工程师，以最佳路径实现需求
5. 千万不要编制模拟操作，我需要能基于 wdp 相关服务真正可交互的系统 demo
6. 需要人工处理的部分随时中断任务咨询我的意见
```

---

## 三、重要概念理解（本轮更新）

### 1. 文档职责分工（关键）

本库采用三层文档架构：

| 文档类型 | 职责 | 使用方式 |
|---------|------|---------|
| **Entry Agent** | 路由判断、执行流程、基线检查 | 每次任务开始时阅读 |
| **Sub Skill** | 能力说明、使用场景、依赖关系 | 根据路由按需阅读 |
| **official-*.md** | 具体 API 签名、返回结构、代码示例（唯一真值） | 代码生成前**必须**阅读 |

**⚠️ 重要**：
- Sub Skill（如 wdp-api-bim-unified）**不提供**具体代码示例
- 代码生成前**必须**查阅对应的 official-*.md 文档
- 禁止基于 Skill 描述自行推理代码

### 2. 视觉反馈选型指南（新增）

如果需求涉及"高亮"、"选中"、"描边"等视觉反馈，需要明确选型：

| 高亮类型 | 使用 Skill | 方法 | 适用场景 |
|---------|-----------|------|---------|
| **模型材质高亮** | wdp-api-material-settings | `SetEntitySlotsHighlight` | 精确控制模型材质 |
| **BIM 构件高亮** | wdp-api-bim-unified | `SetNodeHighLight` | BIM 模型内部构件 |
| **BIM 房间高亮** | wdp-api-bim-unified | `SetRoomHighLight` | BIM 房间/空间 |
| **实体高亮** | wdp-api-entity-general-behavior | `SetEntityHighlight` | 普通实体整体高亮 |
| **实体描边** | wdp-api-entity-general-behavior | `SetEntityOutline` | 实体边缘轮廓 |
| **Tiles 节点高亮** | wdp-api-layer-models | `SetNodesHighlight` | AES 底板单体 |
| **Tiles 图层高亮** | wdp-api-layer-models | `SetLayersHighlight` | AES 底板图层 |

**选型建议**：
- 涉及 BIM 模型 → 使用 wdp-api-bim-unified
- 涉及材质精确控制 → 使用 wdp-api-material-settings
- 涉及普通实体 → 使用 wdp-api-entity-general-behavior
- 涉及 AES 底板 → 使用 wdp-api-layer-models

### 3. 版本基线（更新）

当前库的版本基线：
- **WDP API**: 2.3.0
- **BIM API**: 2.2.0
- **GIS API**: 2.1.0

使用模板时，确保 package.json 中的版本与此一致。

---

## 四、调试与排障

### 1. 代码运行问题

如果代码运行后出现各种问题：
1. 在浏览器控制台（F12 > Console）导出所有 log
2. 让 AI 读取后分析修改

### 2. 经验固化

在正确修复后，可以输入指令固化经验：

```
把这个经验固化下，先判断是放在哪个 api skill 中，确定合理性后进行固化。

如果涉及到新增 skill，注意在入口文件中增加相关索引，尽量沿用已有 skill。
```

**注意**：修改后文件不要进行提交，可以与维护者沟通上传合并。

### 3. 陷入死局时的处理

如果 AI 无法解决问题：
1. 提醒 AI 先阅读 `skills/wdp-entry-agent/SKILL.md`
2. 要求 AI 访问在线文档，检查编写逻辑是否存在出入
3. 要求 AI 输出当前出问题模块的源码，提交给维护者查看

---

## 五、使用建议

### 1. 多次提醒核心规则

使用过程中，可以多次提醒 AI：
- "先阅读 `skills/wdp-entry-agent/SKILL.md`，这是这个库中最为核心的规则"
- "代码生成前必须查阅对应的 official-*.md 文档"

### 2. 主动打断纠正

注意最终使用的人还是你，当看到 AI 明显在做出不合理的举措时，要**打断纠正**。

本库只是尽可能减少错误以及让一般用户用自然语言快速使用 wdp 相关服务，但**无法做到 100% 成功率全自动生成**。


---

## 六、快速参考卡片

### 常用路由速查

| 需求类型 | 目标 Skill |
|---------|-----------|
| 启动/接入失败 | wdp-api-initialization-unified |
| 事件不触发 | wdp-api-general-event-registration |
| 镜头/相机问题 | wdp-api-camera-unified |
| 实体检索/显隐/删除 | wdp-api-entity-general-behavior |
| 覆盖物（POI/Window/热力图） | wdp-api-coverings-unified |
| 底板图层/单体控制 | wdp-api-layer-models |
| 材质替换/高亮 | wdp-api-material-settings |
| 点聚合 | wdp-api-cluster |
| 功能组件（天气/水面等） | wdp-api-function-components |
| BIM 模型/构件/空间 | wdp-api-bim-unified |
| GIS 图层 | gis-api-core-operations |
| 空间理解/坐标转换 | wdp-api-spatial-understanding |

### 官方文档索引

所有 official-*.md 文件位于：`skills/official_api_code_example/`

- `official-initialize-scene.md` - 场景初始化
- `official-general-event-registration.md` - 事件注册
- `official-scene-camera.md` - 相机操作
- `official-generic-base-attributes.md` - 基础属性
- `official-entity-general-behavior.md` - 实体行为
- `official-entity-coverings.md` - 覆盖物
- `official-layer-models.md` - 图层模型
- `official-material-settings.md` - 材质设置
- `official-cluster.md` - 点聚合
- `official-function-components.md` - 功能组件
- `official-bim-full.md` - BIM 完整文档
- `official-gis-full.md` - GIS 完整文档

---

## 七、更新日志

### 2024-04-01 本轮更新
1. 所有 Skill 添加"📋 本文档职责范围"声明
2. 明确代码生成前必须查阅 official-*.md
3. 添加视觉反馈选型指南（高亮方法边界）
4. 更新版本基线（WDP API 2.3.0, BIM API 2.2.0, GIS API 2.1.0）
5. 合并 BIM 文档（official-bim-core-operations → official-bim-full）
6. 优化 Universal Bootstrap 模板

---

**最后提醒**：本库持续优化中，如有问题请及时反馈给维护者。
