在ai开发环境中按需求输入“ ”中的指令：
1）默认加载、启动工作库：
“
- 阅读`\WorkFiles_Codex\WDP_AIcoding\skills\wdp-entry-agent\SKILL.md`，了解路由规则和调度机制
”

2）如果需要根据需求生成demo使用以下指令，注意ai对于我们空间能力很弱，相关参数需要给出具体坐标；如果涉及对象的，必须给出eid或nodeid等信息，或可针对ai给出的需求补充信息进行补充描述，以下为参考：
“
- 先使用'\WDP_AIcoding\skills\wdp-intent-orchestrator'帮我结构化梳理下在”XXXXX“路径下的”XXXX“需求文件，并输出一份结构化梳理完成的《系统意图与架构设计报告》.md文件，输出在”XXXXX“路径下，请注意不要在输出后立刻执行代码的编写，需要进一步询问我是否有特定的实现方式。
”

生成梳理过的结构化需求文档后，使用以下指令：
“
- 读取生成的需求分析文件，先进行编码实现规划，等我确认后，再开始执行编码工作，注意不要拘泥于使用某一技术或框架，结合相关的api-skill进行验证，最后给出实现方式的框架，然后作为一名专业的前端工程师，以最佳路径实现需求，千万不要编制模拟操作，我需要能基于wdp相关服务真正可交互的系统demo。
”

Tips：
1）如果代码运行后出现各种问题，在浏览器控制台（f12>console）导出所有log，并让ai读取后分析后修改代码。
2）可以在正确修复后，输入指令：”把这个经验固化下，先判断是放在哪个api skill中，确定合理性后进行固化，如果涉及到新增skill，注意在入口文件中增加相关索引，尽量沿用已有skill“，但注意修改后文件不要进行提交，可以与我沟通上传合并，代码库未公开、未付费使用高级功能，不支持真正意义上的团队管理[捂脸]
3）注意使用时，可以多次提醒先阅读'skills/wdp-entry-agent/SKILL.md'，这是这个库中最为核心的规则
4）注意最终使用的人还是你，当看到ai明显在做出不合理的举措时，要打断纠正，本库只是尽可能减少错误以及让一般用户用自然语言快速使用wdp相关服务，但无法做到100%成功率全自动生成，
5）工作库可以访问在线文档，陷入死局时可以要求访问在线文档，看下编写逻辑是否存在出入。
6）如果还是无法解决，就要求输出当前出问题模块的源码，提交给我们查看

建议使用Model：
不限制，对上下文本规模支持度高的都可以，近期使用kimi 2.5效果也不错