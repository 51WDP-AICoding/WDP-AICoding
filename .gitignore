# runtime artifacts
feasibility_test_wdp_camera/log/*.log

# temp workspace files
.tmp/
*.tmp

# local IDE config (personal, not shared)
.vscode/

# internal case acquisition artifacts (offline skill tuning only, not for runtime)
skills/example/trueProjects/
